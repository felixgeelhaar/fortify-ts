# npm configuration for publishing

# Enable provenance for npm packages (requires npm 9.5+)
provenance=true

# Public access for scoped packages
access=public

# Use exact versions when installing
save-exact=true
